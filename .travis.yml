branches:
  only: 
    - master

language: python
matrix:
  include:
    - python: "3.5"
    - python: "3.6"
    - python: "3.7"
      dist: xenial
      sudo: true
install:
  - poetry install
script:
  - poetry run pytest tests.py
